name: mod_wsgi
# version_tests: skip_workflow

on: [pull_request, workflow_dispatch]

jobs:
  ubuntu-18:
    if: ${{ vars.CURRENT_TOOL == github.workflow }}
    uses: ./.github/workflows/build-ubuntu.yml
    with:
      workflow: ${{ github.workflow }}
      version: 18.04
