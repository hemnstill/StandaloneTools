on:
  workflow_call:
    secrets:
      token:
        required: true
jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/download-artifact@v3
        with:
          path: .
      - name: Display structure of downloaded files
        run: ls -R
