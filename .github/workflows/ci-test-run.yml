name: ci build

on: [pull_request]

jobs:
  alpine-docker:
    runs-on: ubuntu-latest
    container: alpine:3.15.0

    steps:
      - uses: actions/checkout@v2

      - name: build bsdtar
        run: ./bsdtar/build.sh