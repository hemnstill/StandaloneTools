name: far2l

on: [pull_request, workflow_dispatch]

jobs:
  test-run:
    uses: ./.github/workflows/prejob.yml
    with:
      workflow: ${{ github.workflow }}
    secrets:
      _WORKFLOW: ${{ secrets._CURRENT_TOOL }}

  ubuntu-far2l-linux:
    needs: test-run
    runs-on: ubuntu-latest
    steps:
      - run: echo ${{ needs.prejob.outputs.workflow }}
